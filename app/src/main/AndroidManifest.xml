<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.freyja">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Freyja"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Freyja">

        <activity android:name=".RequestsActivity" />
        <activity android:name=".MessagesActivity" />
        <activity android:name=".PaymentsActivity" />
        <activity android:name=".DebtActivity" />
        <activity android:name=".ResidentHomeActivity" />
        <activity android:name=".MakePaymentActivity" android:exported="false" />
        <activity android:name=".PollsActivity" android:exported="false"/>
        <activity android:name=".PollDetailActivity" android:exported="false"/>
        <activity android:name=".PollResultsActivity" android:exported="false"/>

        <activity android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
